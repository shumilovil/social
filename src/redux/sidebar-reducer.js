const initialState = {
  friends: [
    { id: 1, name: 'Sham1' },
    { id: 2, name: 'Sham2' },
    { id: 3, name: 'Sham3' }
  ]
};

const sidebarReducer = (state = initialState, action) => {
  return state;
}

export default sidebarReducer;